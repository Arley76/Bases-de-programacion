//Un bucle while que cuente desde 50 hasta 40 (fíjate que en este caso es decreciente)
 let x =50
while (x >= 40) {
    console.log (`iteracion, ${x}`)
    x--
}
console.log("Fin del ciclo")  
/* for (let num = 50; num >= 40; num--) {
     console.log(`iteracion ${num}`)
}
console.log(`Fin del ciclo`);
 */
